<h1>
  My first theme!
</h1>

<img src="{{ 'puppy.jpg' | asset_url }}" />

<h2>{{ collections['frontpage'].products }}</h2>

{% for product in collections['frontpage'].products %}
  {% render 'product-item', product: product %}
{% endfor %}